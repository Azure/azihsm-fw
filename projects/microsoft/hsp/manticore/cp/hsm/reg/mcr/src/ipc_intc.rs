// Copyright (c) Microsoft Corporation. All rights reserved.
//
// generated by mcr-reg-gen from mcr-cp-ipc-reg.xml

#![allow(clippy::erasing_op)]
#![allow(clippy::identity_op)]
pub mod regs {
    //! Types that represent the values held by registers.
}
pub mod enums {
    //! Enumerations used by some register fields.
    pub mod selector {}
}
pub mod meta {
    //! Additional metadata needed by ureg.
}
pub mod ipc_intc_int_registers {
    #[derive(Clone, Copy)]
    pub struct RegisterBlock<TMmio: ureg::Mmio + core::borrow::Borrow<TMmio> = ureg::RealMmio> {
        ptr: *mut u32,
        mmio: TMmio,
    }
    impl RegisterBlock<ureg::RealMmio> {
        pub fn block() -> Self {
            unsafe { Self::new(0xb0006000 as *mut u32) }
        }
    }
    impl<TMmio: ureg::Mmio + core::default::Default> RegisterBlock<TMmio> {
        /// # Safety
        ///
        /// The caller is responsible for ensuring that ptr is valid for
        /// volatile reads and writes at any of the offsets in this register
        /// block.
        pub unsafe fn new(ptr: *mut u32) -> Self {
            Self {
                ptr,
                mmio: core::default::Default::default(),
            }
        }
    }
    impl<TMmio: ureg::Mmio> RegisterBlock<TMmio> {
        /// # Safety
        ///
        /// The caller is responsible for ensuring that ptr is valid for
        /// volatile reads and writes at any of the offsets in this register
        /// block.
        pub unsafe fn new_with_mmio(ptr: *mut u32, mmio: TMmio) -> Self {
            Self { ptr, mmio }
        }
        pub fn at(&self, index: usize) -> IpcIntcIntRegistersBlock<&TMmio> {
            assert!(index < 6usize);
            IpcIntcIntRegistersBlock {
                ptr: unsafe { self.ptr.add((0 + index * 4) / core::mem::size_of::<u32>()) },
                mmio: core::borrow::Borrow::borrow(&self.mmio),
            }
        }
    }
    #[derive(Clone, Copy)]
    pub struct IpcIntcIntRegistersBlock<TMmio: ureg::Mmio> {
        ptr: *mut u32,
        mmio: TMmio,
    }
    impl<TMmio: ureg::Mmio> IpcIntcIntRegistersBlock<TMmio> {
        /// CPU n Interrupt Pending Clear.
        ///
        /// Read value: [`u32`]; Write value: [`u32`]
        #[inline(always)]
        pub fn int_pend_clr(
            &self,
        ) -> ureg::RegRef<
            crate::ipc_intc::ipc_intc_int_registers::meta::IpcIntcIntRegistersIntPendClr,
            &TMmio,
        > {
            unsafe {
                ureg::RegRef::new_with_mmio(
                    self.ptr.wrapping_add(0 / core::mem::size_of::<u32>()),
                    core::borrow::Borrow::borrow(&self.mmio),
                )
            }
        }
        /// CPU n Interrupt Pending Set.
        ///
        /// Read value: [`u32`]; Write value: [`u32`]
        #[inline(always)]
        pub fn int_pend_set(
            &self,
        ) -> ureg::RegRef<
            crate::ipc_intc::ipc_intc_int_registers::meta::IpcIntcIntRegistersIntPendSet,
            &TMmio,
        > {
            unsafe {
                ureg::RegRef::new_with_mmio(
                    self.ptr.wrapping_add(0x18 / core::mem::size_of::<u32>()),
                    core::borrow::Borrow::borrow(&self.mmio),
                )
            }
        }
        /// CPU n Interrupt Mask Clear.
        ///
        /// Read value: [`u32`]; Write value: [`u32`]
        #[inline(always)]
        pub fn int_mask_clr(
            &self,
        ) -> ureg::RegRef<
            crate::ipc_intc::ipc_intc_int_registers::meta::IpcIntcIntRegistersIntMaskClr,
            &TMmio,
        > {
            unsafe {
                ureg::RegRef::new_with_mmio(
                    self.ptr.wrapping_add(0x30 / core::mem::size_of::<u32>()),
                    core::borrow::Borrow::borrow(&self.mmio),
                )
            }
        }
        /// CPU n Interrupt Mask Set.
        ///
        /// Read value: [`u32`]; Write value: [`u32`]
        #[inline(always)]
        pub fn int_mask_set(
            &self,
        ) -> ureg::RegRef<
            crate::ipc_intc::ipc_intc_int_registers::meta::IpcIntcIntRegistersIntMaskSet,
            &TMmio,
        > {
            unsafe {
                ureg::RegRef::new_with_mmio(
                    self.ptr.wrapping_add(0x48 / core::mem::size_of::<u32>()),
                    core::borrow::Borrow::borrow(&self.mmio),
                )
            }
        }
        /// CPU n Interrupt Enable Clear.
        ///
        /// Read value: [`u32`]; Write value: [`u32`]
        #[inline(always)]
        pub fn int_enable_clr(
            &self,
        ) -> ureg::RegRef<
            crate::ipc_intc::ipc_intc_int_registers::meta::IpcIntcIntRegistersIntEnableClr,
            &TMmio,
        > {
            unsafe {
                ureg::RegRef::new_with_mmio(
                    self.ptr.wrapping_add(0x60 / core::mem::size_of::<u32>()),
                    core::borrow::Borrow::borrow(&self.mmio),
                )
            }
        }
        /// CPU n Interrupt Enable Set.
        ///
        /// Read value: [`u32`]; Write value: [`u32`]
        #[inline(always)]
        pub fn int_enable_set(
            &self,
        ) -> ureg::RegRef<
            crate::ipc_intc::ipc_intc_int_registers::meta::IpcIntcIntRegistersIntEnableSet,
            &TMmio,
        > {
            unsafe {
                ureg::RegRef::new_with_mmio(
                    self.ptr.wrapping_add(0x78 / core::mem::size_of::<u32>()),
                    core::borrow::Borrow::borrow(&self.mmio),
                )
            }
        }
    }
    pub mod regs {
        //! Types that represent the values held by registers.
    }
    pub mod enums {
        //! Enumerations used by some register fields.
        pub mod selector {}
    }
    pub mod meta {
        //! Additional metadata needed by ureg.
        pub type IpcIntcIntRegistersIntPendClr = ureg::ReadWriteReg32<0, u32, u32>;
        pub type IpcIntcIntRegistersIntPendSet = ureg::ReadOnlyReg32<u32>;
        pub type IpcIntcIntRegistersIntMaskClr = ureg::ReadWriteReg32<0, u32, u32>;
        pub type IpcIntcIntRegistersIntMaskSet = ureg::ReadWriteReg32<0, u32, u32>;
        pub type IpcIntcIntRegistersIntEnableClr = ureg::ReadWriteReg32<0, u32, u32>;
        pub type IpcIntcIntRegistersIntEnableSet = ureg::ReadWriteReg32<0, u32, u32>;
    }
}
pub mod ipc_intc_descriptor_registers {
    #[derive(Clone, Copy)]
    pub struct RegisterBlock<TMmio: ureg::Mmio + core::borrow::Borrow<TMmio> = ureg::RealMmio> {
        ptr: *mut u32,
        mmio: TMmio,
    }
    impl RegisterBlock<ureg::RealMmio> {
        pub fn block() -> Self {
            unsafe { Self::new(0xb0006110 as *mut u32) }
        }
    }
    impl<TMmio: ureg::Mmio + core::default::Default> RegisterBlock<TMmio> {
        /// # Safety
        ///
        /// The caller is responsible for ensuring that ptr is valid for
        /// volatile reads and writes at any of the offsets in this register
        /// block.
        pub unsafe fn new(ptr: *mut u32) -> Self {
            Self {
                ptr,
                mmio: core::default::Default::default(),
            }
        }
    }
    impl<TMmio: ureg::Mmio> RegisterBlock<TMmio> {
        /// # Safety
        ///
        /// The caller is responsible for ensuring that ptr is valid for
        /// volatile reads and writes at any of the offsets in this register
        /// block.
        pub unsafe fn new_with_mmio(ptr: *mut u32, mmio: TMmio) -> Self {
            Self { ptr, mmio }
        }
        pub fn at(&self, index: usize) -> IpcIntcDescriptorRegistersBlock<&TMmio> {
            assert!(index < 32usize);
            IpcIntcDescriptorRegistersBlock {
                ptr: unsafe { self.ptr.add((0 + index * 4) / core::mem::size_of::<u32>()) },
                mmio: core::borrow::Borrow::borrow(&self.mmio),
            }
        }
    }
    #[derive(Clone, Copy)]
    pub struct IpcIntcDescriptorRegistersBlock<TMmio: ureg::Mmio> {
        ptr: *mut u32,
        mmio: TMmio,
    }
    impl<TMmio: ureg::Mmio> IpcIntcDescriptorRegistersBlock<TMmio> {
        /// IPC Descriptor Register n.
        ///
        /// Read value: [`u32`]; Write value: [`u32`]
        #[inline(always)]        pub fn ipc_desc_reg_n (& self) -> ureg :: RegRef < crate :: ipc_intc :: ipc_intc_descriptor_registers :: meta :: IpcIntcDescriptorRegistersIpcDescRegN , & TMmio >{
            unsafe {
                ureg::RegRef::new_with_mmio(
                    self.ptr.wrapping_add(0 / core::mem::size_of::<u32>()),
                    core::borrow::Borrow::borrow(&self.mmio),
                )
            }
        }
    }
    pub mod regs {
        //! Types that represent the values held by registers.
    }
    pub mod enums {
        //! Enumerations used by some register fields.
        pub mod selector {}
    }
    pub mod meta {
        //! Additional metadata needed by ureg.
        pub type IpcIntcDescriptorRegistersIpcDescRegN = ureg::ReadWriteReg32<0, u32, u32>;
    }
}
